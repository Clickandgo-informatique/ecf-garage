{% extends "base.html.twig" %}
{% block title %}Créer un utilisateur
{% endblock %}
{% block body %}
	<main>
		<h1 class="titre-principal">Créer un utilisateur</h1>
		{{form_start(user)}}
		{{form_row(user.nom)}}
		<button class="btn btn-primary" type="submit">Enregistrer</button>

		{{form_end(user)}}
	</main>
{% endblock %}
