{% extends "base.html.twig" %}
{% block title %}Créer un utilisateur
{% endblock %}

{% block body %}
	<div class="wrapper">
		{{ include('_partials/_admin-nav.html.twig') }}
		<main>

			{{form_start(form)}}
			<div class="form-header">Créer un utilisateur</div>
			{{form_row(form.nom)}}
			{{form_row(form.prenom)}}
			{{form_row(form.email)}}
			{{form_row(form.adresse)}}
			{{form_row(form.code_postal)}}
			{{form_row(form.ville)}}
			{{form_row(form.tel_fixe)}}
			{{form_row(form.tel_mobile)}}
			{{form_row(form.password)}}
			{{form_row(form.roles)}}
			{{form_row(form.created_at)}}
			<button class="btn btn-primary btn-centered" type="submit">Enregistrer</button>

			{{form_end(form)}}

		</main>
	</div>
{% endblock %}
